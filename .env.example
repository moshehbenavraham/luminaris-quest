# Supabase Configuration (REQUIRED FOR ALL ENVIRONMENTS)
#
# IMPORTANT: There are two local development scenarios:
#
# 1. LOCAL DEVELOPMENT → PRODUCTION DATABASE (Recommended, Default)
#    - Run app on localhost:8080
#    - Connect to production/staging Supabase database
#    - Best for most development work
#    - Setup: Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY below
#    - DO NOT set VITE_LOCAL_SUPABASE_URL variables
#
# 2. LOCAL DEVELOPMENT → LOCAL SUPABASE DATABASE (Advanced)
#    - Run app on localhost:8080
#    - Connect to local Supabase instance (requires Supabase CLI)
#    - Best for testing migrations or working offline
#    - Setup: Set VITE_LOCAL_SUPABASE_URL and VITE_LOCAL_SUPABASE_ANON_KEY below
#    - See: docs/guides/deployment.md#local-supabase-instance
#
# Get these from your Supabase project settings
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: Local Supabase Instance Override (Advanced Users Only)
# ONLY set these if running a local Supabase instance via Supabase CLI
# If these are NOT set, localhost will connect to the production URL above
# VITE_LOCAL_SUPABASE_URL=http://localhost:54321
# VITE_LOCAL_SUPABASE_ANON_KEY=your-local-anon-key

# Optional: AI Features (Planned, Not Currently Used)
# OpenAI Configuration (for future AI narrative generation)
# VITE_OPENAI_API_KEY=your-openai-api-key

# Leonardo.AI Configuration (for future image generation)
# VITE_LEONARDO_API_KEY=your-leonardo-api-key

# ElevenLabs Configuration (for future voice synthesis)
# VITE_ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Deployment Configuration
# VITE_APP_URL=http://localhost:8080  # Development
VITE_APP_URL=https://luminarisquest.org/  # Production

# Feature Flags
VITE_ENABLE_AI_NARRATION=true
VITE_ENABLE_IMAGE_GENERATION=true
VITE_ENABLE_VOICE_SYNTHESIS=true

# Optional: Development Settings
VITE_DEBUG_MODE=false
VITE_MOCK_API_RESPONSES=false  # For development without API keys